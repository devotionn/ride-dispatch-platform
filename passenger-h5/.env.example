# Backend API base. Leave empty when H5 and API share the same origin or when using Vite dev proxy.
VITE_API_BASE_URL=

# Optional local Vite proxy target; defaults to http://localhost:8080.
VITE_DEV_API_TARGET=
